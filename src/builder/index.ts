/**
 * Builder module barrel.
 *
 * Re-exports the full AST-native builder surface:
 * - typed expression/context APIs
 * - database shape and path utility types
 * - helper registration contracts
 * - rules builder entrypoints
 */
export * from "./context"
export * from "./db"
export * from "./helpers"
export * from "./rules-builder"
